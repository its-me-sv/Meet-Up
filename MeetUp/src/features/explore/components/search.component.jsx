import React from "react";

import {
    SearchContainer,
    SearchBar as Searchbar
} from "./search.styles";

const SearchBar = () => {
    return (
        <SearchContainer>
            <Searchbar />
        </SearchContainer>
    );
};

export default SearchBar;